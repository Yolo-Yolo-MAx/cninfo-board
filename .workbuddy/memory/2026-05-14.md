# 2026-05-14

## 修复：今日新增显示昨天数据（5.14早上）

**问题**：5.14早上 10:41，今日新增显示 4 条昨天（5.13）的数据。

**根本原因**：`cninfo_crawler_v2.py` 的 `if not unique:` 分支（无新公告时）直接 `return`，跳过了 `build_html.py`，导致 `index.html` 停留在昨天 slot 18/24 生成的版本。而凌晨 slot 0 已经正确重置了 `announcements_today.json`（date=2026-05-14, records=[]），但 `index.html` 嵌入的仍是旧数据。

**修复**：
1. 将 `build_html.py` 逻辑抽取为 `build_index_html()` 函数，内联到 crawler 中
2. 两个分支（有新公告/无新公告）都调用 `build_index_html()`
3. 手动运行 `build_html.py` 重新生成 `index.html`（今日新增=0）
4. 提交推送至 Gitee（成功），GitHub 被 Push Protection 拦截（历史 commit 含 Personal Access Token），需用户到 GitHub 解除限制后才能推送
