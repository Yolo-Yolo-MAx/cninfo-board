## 2026-05-07

### iFinD MCP 集成到爬虫

- 安装 iFinD skill 到 `~/.workbuddy/skills/ifind-finance-data/`
- Python venv: `~/.workbuddy/binaries/python/envs/default/`（需用 Scripts/python.exe）
- 依赖: `requests`（managed Python 环境需先装）

### cninfo_crawler_v2.py 更新
- 行业数据源: 东方财富 → iFinD MCP（`get_stock_info`，查询 `所属同花顺行业` + `主营业务`）
- 新增 `fmt_code()`: 股票代码转 iFinD 格式（000858→000858.SZ, 600519→600519.SH）
- 新增 `parse_md_table()`: 解析 iFinD 返回的 markdown 表格（按位置索引，兼容表头/数据行长度不一致）
- iFinD token 已写入 `mcp_config.json`

### backfill_industry.py 更新
- 同步替换为 iFinD 版本

### 技术细节
- iFinD 表格列数: 表头9个元素(含头尾空串)，数据行9个元素（因表格末有trailing `|`）
- 解析用 `header[i]` 非空才取 `data[i]`，避免空列错位
- iFinD 某些股票无行业/主营业务字段时返回空，正常行为
