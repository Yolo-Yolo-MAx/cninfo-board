# 2026-05-08 日志

## 巨潮爬虫更新

### 修复：index.html "加载中" 一直不显示
- 根因：`index_v2.html` 第249-250行占位符是 `__ALL_RECORDS__`（无引号），而 `build_html.py` 替换的是 `"__ALL_RECORDS__"`（有引号），两边不匹配导致替换失败
- 修复：把 `index_v2.html` 的占位符改为带引号的 `"__ALL_RECORDS__"` 和 `"__TODAY_RECORDS__"`
- 同时修复 `cninfo_crawler_v2.py` 里的 `datetime.datetime.fromtimestamp` 错误

### 重构：时间槽逻辑
- 爬虫支持命令行参数：`python cninfo_crawler_v2.py [12|18|24]`
- 12点槽：抓 00:00~12:00 的公告
- 18点槽：抓 12:00~18:00 的公告
- 24点槽：抓 18:00~23:59 的公告
- 今日记录（announcements_today.json）全天累积：12点后=3条，18点后=累计6条，24点后=全天完整
- 自动化已设置：12:00 / 18:00 / 00:00（自动化ID: automation-1778173700839/0845/0829）

### 关键代码位置
- 爬虫入口：`cninfo_crawler_v2.py` main()
- HTML模板：`index_v2.html`（用 build_html.py 从此模板生成 index.html）
- 数据文件：`data/announcements_all.json`，`data/announcements_today.json`
- 飞书Token：写死在代码里

### 注意
- `build_html.py` 在 `cninfo_crawler_v2.py` 末尾调用，在 save_records_to_json 之后
- 飞书已有记录的去重判断：`(stock_code, title)` 组合

---

## 定时任务执行记录

### 12:00 槽（2026-05-08 12:00）
- 时段: 00:00~12:00，符合条件 3 条
  - [重整] 002360 ST同德（基础化工-化学制品-民爆制品）
  - [拍卖] 000533 顺钠股份（电力设备-电网设备）
  - [拍卖] 002072 凯瑞德（商贸零售-贸易Ⅱ-贸易Ⅲ）
- 飞书：累计 11 条，今日 3 条（无新增，已存在）
- GitHub 推送：成功（已最新）
- 飞书通知：发送成功
